import React from 'react';

const Loading = () => {
    return (
        <div className="loading-body flex-center">
            Loading <span>.</span><span>.</span><span>.</span>
        </div>
    );
}

export default Loading;